<div class="d-flex align-items-center justify-content-center">
  <div class="text-start">
    <div class="form-list new-dog-form">
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email" } %>
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password" } %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>
        <div class="form-actions" style="position: relative;">
          <button class="button-55" style="height: 2.25rem; margin-bottom: 10px; width: 70px;"><%= f.button :submit, "Log in", style: "position: relative; left: -9px; background-color: white;" %></button>
        </div>
      <% end %>
    </div>
  </div>
</div>
